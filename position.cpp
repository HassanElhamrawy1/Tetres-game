#include "position.h"

Position::Position(int row, int column)
{
    this -> row = row;                   /* this will refer to the current object */
    this -> column = column;             /* the arrow will be used to access a variable within the object */

}












